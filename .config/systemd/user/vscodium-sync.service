[Unit]
Description=Sync VSCodium extensions from extensions.list
After=dotfiles-sync.service

[Service]
Type=oneshot
ExecStart=/bin/bash -c '[[ -x "$HOME/.config/dotfiles/install-extensions.sh" ]] && "$HOME/.config/dotfiles/install-extensions.sh"'
StandardOutput=journal
StandardError=journal

[Install]
WantedBy=graphical-session.target
